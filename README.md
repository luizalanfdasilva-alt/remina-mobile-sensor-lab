<html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover,user-scalable=no"><meta name="theme-color" content="#071018"><meta name="description" content="REMINA Mobile Sensor Lab V4.1 — percepção, acessibilidade, contexto, navegação e resposta adaptativa."><title>REMINA Mobile Sensor Lab V4.1</title>
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
<style>
:root{--bg:#061018;--panel:#0b1720;--panel2:#10232e;--line:#21404e;--txt:#f4fbff;--muted:#9db2bd;--gold:#e9bd55;--cyan:#50d7e8;--green:#4de39a;--orange:#ffae58;--red:#ff6464}*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}html,body{margin:0;background:radial-gradient(circle at 50% 0,#12303b 0,#061018 45%,#03080c 100%);color:var(--txt);font-family:system-ui,-apple-system,Segoe UI,sans-serif}body{padding-bottom:76px}.app{width:min(100%,760px);margin:auto;padding:10px}.top{position:sticky;top:0;z-index:50;background:#061018eF;backdrop-filter:blur(14px);border-bottom:1px solid var(--line);padding:10px 4px}.brand{display:flex;justify-content:space-between;gap:10px;align-items:center}.brand h1{font-size:18px;margin:0}.brand small{color:var(--muted)}.dot{display:inline-block;width:9px;height:9px;border-radius:50%;background:var(--red);margin-right:5px}.dot.on{background:var(--green)}.card{background:#091721e8;border:1px solid var(--line);border-radius:18px;padding:12px;margin:10px 0;box-shadow:0 10px 30px #0007}.hero{padding:14px}.hero h2{font-size:28px;line-height:1;margin:4px 0 8px}.hero p,.sub{color:var(--muted);font-size:12px;line-height:1.45}button,input,textarea,select{font:inherit}button{border:1px solid var(--line);background:var(--panel2);color:var(--txt);border-radius:13px;padding:11px 12px;font-weight:800}button:active{transform:scale(.98)}button.primary{background:var(--gold);color:#101010;border-color:var(--gold)}button.good{border-color:#2c815f;background:#103d2f}button.warn{border-color:#805c2b;background:#3b2a12}button.small{padding:8px 10px;font-size:12px}button:disabled{opacity:.45}.row{display:flex;gap:8px;flex-wrap:wrap}.row>*{flex:1}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.metric{background:#ffffff06;border:1px solid #ffffff12;border-radius:12px;padding:9px;text-align:center}.metric b{display:block;font-size:16px}.metric span{font-size:10px;color:var(--muted)}.camera{padding:0;overflow:hidden}.viewport{position:relative;min-height:500px;background:#02070a}.viewport video{width:100%;height:100%;min-height:500px;object-fit:cover;display:block;background:#02070a}.cameraGate{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;text-align:center;background:linear-gradient(#0008,#0004);padding:30px}.cameraGate.hidden{display:none}.cameraGate div{max-width:320px}.hud{position:absolute;top:9px;left:9px;right:9px;display:flex;gap:6px;flex-wrap:wrap;z-index:3}.badge{background:#061018dd;border:1px solid #ffffff25;border-radius:999px;padding:6px 9px;font-size:11px;color:#fff}.badge.on{border-color:#45d79899}.cameraControls{position:absolute;left:8px;right:8px;bottom:8px;z-index:4;display:flex;justify-content:space-between;align-items:flex-end}.side{display:flex;flex-direction:column;gap:7px}.round{width:48px;height:48px;border-radius:50%;padding:0;background:#061018dd}.capture{width:72px;height:72px;border-radius:50%;padding:0;border:5px solid #fff;background:#ffffffcc;box-shadow:0 0 0 5px #ffffff33}.overlay{position:absolute;inset:0;pointer-events:none;z-index:2}.access{position:absolute;pointer-events:auto;min-width:76px;min-height:64px;background:#061018d9;border-color:#ffffff2b;font-size:10px}.access b{display:block;font-size:20px}.a1{top:76px;left:8px}.a2{top:76px;right:8px}.a3{top:155px;left:8px}.a4{top:155px;right:8px}.a5{top:234px;left:8px}.a6{top:234px;right:8px}.gpsCenter{position:absolute;left:50%;top:335px;transform:translateX(-50%);pointer-events:auto;width:84px;height:84px;border-radius:50%;background:#071a24e8;border:2px solid var(--cyan);color:#fff}.routeSvg{position:absolute;inset:0;width:100%;height:100%;pointer-events:none}.routeLine{fill:none;stroke:var(--gold);stroke-width:8;stroke-linecap:round;stroke-linejoin:round;stroke-dasharray:22 14;filter:drop-shadow(0 2px 3px #000)}.routeDot{fill:var(--cyan);stroke:#fff;stroke-width:3}.modal{position:fixed;inset:0;background:#000b;z-index:100;display:none;align-items:flex-end}.modal.show{display:flex}.sheet{width:min(100%,760px);margin:auto;background:#091721;border:1px solid var(--line);border-radius:20px 20px 0 0;padding:14px;max-height:85vh;overflow:auto}.options{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.option{text-align:left;min-height:70px}.experienceBox{display:grid;gap:9px}.experienceBox textarea{min-height:150px;font-size:15px;line-height:1.5}.experienceHint{background:#ffffff06;border:1px solid #ffffff12;border-radius:12px;padding:10px;color:var(--muted);font-size:11px;line-height:1.45}.experienceHint b{color:var(--txt)}.option small{display:block;color:var(--muted);font-weight:500;margin-top:3px}.log{font-family:ui-monospace,monospace;font-size:10px;color:#b7c8d0;background:#03080c;border-radius:12px;padding:10px;min-height:72px;white-space:pre-wrap}.bar{height:8px;background:#ffffff10;border-radius:20px;overflow:hidden}.bar i{display:block;height:100%;width:0;background:var(--cyan)}textarea{min-height:86px;width:100%;background:#061018;color:#fff;border:1px solid var(--line);border-radius:13px;padding:12px;outline:none}input,select{background:#061018;color:#fff;border:1px solid var(--line);border-radius:12px;padding:11px;width:100%}#map{height:340px;border-radius:14px;overflow:hidden}.pointList{display:grid;gap:7px;margin-top:8px}.point{padding:9px;border:1px solid #ffffff12;border-radius:12px;background:#ffffff05}.point b{font-size:12px}.point small{display:block;color:var(--muted)}.routeCard{border:1px solid #50d7e855;background:#50d7e80b;padding:10px;border-radius:13px}.tag{display:inline-block;border:1px solid #ffffff18;border-radius:999px;padding:4px 7px;font-size:10px;color:var(--muted);margin:2px}.footer{color:#728893;text-align:center;font-size:10px;padding:15px}.fixed{position:fixed;left:10px;right:10px;bottom:10px;z-index:60}.fixed button{width:100%}.hidden{display:none!important}@media(max-width:430px){.viewport,.viewport video{min-height:440px}.access{min-width:66px}.gpsCenter{top:310px}.metrics{grid-template-columns:repeat(2,1fr)}}
</style><style>
.v41-badge{display:inline-block;margin-left:6px;padding:3px 7px;border-radius:999px;background:#16384a;color:#bdefff;font-size:10px;font-weight:800}.v41-sheet{padding:4px}.v41-sheet textarea{width:100%;min-height:150px;border-radius:14px;padding:14px;font:inherit;background:#07151e;color:#fff;border:1px solid #315264;resize:vertical}.v41-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.v41-card{padding:12px;border:1px solid #234252;border-radius:14px;background:rgba(8,24,33,.78);margin:8px 0}.v41-muted{opacity:.72;font-size:12px}.v41-media{display:flex;gap:8px;flex-wrap:wrap}.v41-media img,.v41-media video{max-width:100%;border-radius:12px}.v41-pill{padding:5px 8px;border-radius:999px;background:#12303e;font-size:11px}.v41-danger{background:#4b2020}.v41-ok{background:#153d2a}</style><link rel="manifest" href="./manifest.json"><meta name="apple-mobile-web-app-capable" content="yes"><meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
</head><body><div class="app">
<header class="top"><div class="brand"><div><h1>REMINA MOBILE SENSOR LAB</h1><small>V4.0 • Perception • Accessibility • Context • Navigation</small></div><div><span id="engineDot" class="dot"></span><span id="engineText">OFF</span></div></div></header>
<section class="card hero"><span class="tag">SINTA DE NOVO • FEEL AGAIN</span><h2>Cada celular pode ser um laboratório sensorial da cidade.</h2><p>Perceber → Registrar → Analisar → Compreender → Sugerir → Orientar → Adaptar → Aprender.</p><div class="row"><button id="initBtn" class="primary">INICIAR LABORATÓRIO</button><button id="helpBtn">AJUDA</button></div><p id="initMsg" class="sub">Local-first: o laboratório continua útil mesmo sem internet.</p></section>
<section class="card camera"><div class="viewport"><video id="camera" autoplay playsinline muted></video><div id="cameraGate" class="cameraGate"><div><h2>📷 CÂMERA SENSORIAL</h2><p>Use a câmera para observar e registrar evidências.</p><button id="openCamera" class="primary">ABRIR CÂMERA</button></div></div><div class="hud"><button id="gpsHud" class="badge">📍 GPS: —</button><span id="lightHud" class="badge">☀️ Luz: —</span><span id="batteryHud" class="badge">🔋 —</span><span id="weatherHud" class="badge">🌦️ —</span></div><div class="overlay"><button class="access a1" onclick="openCategory('Mobilidade')"><b>♿</b>Cadeirante</button><button class="access a2" onclick="openCategory('Orientação')"><b>🦯</b>Cego</button><button class="access a3" onclick="openCategory('Aroma')"><b>🌼</b>Aroma</button><button class="access a4" onclick="openCategory('Saneamento')"><b>💧</b>Paladar</button><button class="access a5" onclick="openCategory('Infraestrutura')"><b>🚧</b>Quebrada</button><button class="access a6" onclick="openCategory('Som')"><b>🔊</b>Som</button><button class="gpsCenter" onclick="openGPS()">📍<br><b>GPS</b><br><small>ROTAS</small></button><svg class="routeSvg" viewBox="0 0 1000 1000" preserveAspectRatio="none"><path id="routeLine" class="routeLine" d=""/><circle id="routeUser" class="routeDot" cx="-50" cy="-50" r="12"/><circle id="routeTarget" class="routeDot" cx="-50" cy="-50" r="12"/></svg></div><div class="cameraControls"><div class="side"><button class="round" id="flipBtn">🔄</button><button class="round" id="torchBtn">🔦</button></div><button class="capture" id="captureBtn" title="Toque: foto"></button><div class="side"><button class="round" id="analyzeBtn">🧠</button><button class="round" id="stopCamBtn">⏹️</button></div></div></div></section>
<section class="card"><h3>🧠 CONTEXTO & IA</h3><div class="metrics"><div class="metric"><b id="aiSituation">—</b><span>Situação</span></div><div class="metric"><b id="aiConfidence">0%</b><span>Confiança</span></div><div class="metric"><b id="aiEvidence">0</b><span>Evidências</span></div></div><p id="aiText" class="sub">A análise local usa texto + sensores + evidências disponíveis. IA externa é opcional e requer conexão/consentimento.</p><div class="row"><button id="contextBtn" class="primary">ANALISAR CONTEXTO</button><button id="adaptiveBtn">🫶 RESPOSTA ADAPTATIVA</button></div><div id="aiActions" class="sub"></div></section>
<section class="card"><h3>🧭 NAVEGAÇÃO REMINA</h3><div id="routeInfo" class="routeCard">Nenhuma rota ativa.</div><div class="row" style="margin-top:8px"><button id="gpsBtn">ATIVAR GPS</button><button id="pointsBtn">PONTOS</button><button id="googleBtn">GOOGLE MAPS</button></div><div class="row" style="margin-top:8px"><button id="audioNavBtn">🔊 ÁUDIO: OFF</button><button id="repeatNavBtn">REPETIR</button><button id="stopRouteBtn">PARAR ROTA</button></div><div id="map"></div><div id="pointList" class="pointList"></div></section>
<section class="card"><h3>🎙️ VOICE-FIRST</h3><p class="sub">Comandos: “Registrar”, “Analisar”, “Encontrar rota”, “Onde estou?”, “Ler rota”, “Repetir”, “Parar”, “Ajuda”.</p><div class="row"><button id="voiceBtn" class="primary">🎙️ FALAR</button><button id="speakBtn">🔊 LER STATUS</button></div><div id="voiceStatus" class="sub">Aguardando voz.</div></section>
<section class="card"><h3>📝 REGISTRAR OCORRÊNCIA</h3><select id="category"><option>Mobilidade</option><option>Orientação</option><option>Aroma</option><option>Saneamento</option><option>Infraestrutura</option><option>Som</option><option>Outro</option></select><textarea id="note" placeholder="Descreva o que você observou…"></textarea><div class="row"><button id="saveBtn" class="primary">SALVAR REGISTRO</button><button id="voiceNoteBtn">NOTA POR VOZ</button></div><p id="recordStatus" class="sub"></p></section>
<section class="card"><h3>🌦️ CLIMA & CONTEXTO</h3><div class="metrics"><div class="metric"><b id="temp">—</b><span>temperatura</span></div><div class="metric"><b id="rain">—</b><span>chuva</span></div><div class="metric"><b id="wind">—</b><span>vento</span></div></div><button id="weatherBtn" style="margin-top:8px">ATUALIZAR CLIMA</button><p class="sub">Clima é contexto, não diagnóstico. A disponibilidade depende da internet.</p></section>
<section class="card"><h3>📊 ACESSIBILIDADE & AUDITORIA</h3><div class="metrics"><div class="metric"><b id="scoreMob">—</b><span>Mobilidade</span></div><div class="metric"><b id="scoreOri">—</b><span>Orientação</span></div><div class="metric"><b id="scoreEnv">—</b><span>Ambiente</span></div></div><div class="bar" style="margin-top:10px"><i id="auditBar"></i></div><p id="auditText" class="sub">O índice representa a qualidade dos dados disponíveis, não uma certificação oficial de acessibilidade.</p></section>
<section class="card"><h3>🗃️ MEMÓRIA SENSORIAL</h3><div class="row"><button id="exportBtn">EXPORTAR JSON</button><button id="clearBtn" class="warn">LIMPAR</button></div><div id="history" class="pointList"></div></section>
<section class="card"><h3>⚙️ ESTADO DO LABORATÓRIO</h3><div class="metrics"><div class="metric"><b id="gpsState">OFF</b><span>GPS</span></div><div class="metric"><b id="motionState">OFF</b><span>Movimento</span></div><div class="metric"><b id="orientationState">OFF</b><span>Orientação</span></div><div class="metric"><b id="audioState">OFF</b><span>Áudio</span></div><div class="metric"><b id="storageState">LOCAL</b><span>Storage</span></div><div class="metric"><b id="netState">—</b><span>Internet</span></div></div><div id="log" class="log">REMINA aguardando inicialização.</div></section><footer class="footer">REMINA SYSTEM • Adaptar o mundo ao ser humano, e não o contrário.<br>Experimental accessibility technology • Non-clinical.</footer></div>
<div id="modal" class="modal"><div class="sheet"><div class="row"><h3 id="modalTitle" style="margin:0;flex:1">Opções</h3><button id="modalClose" class="small">FECHAR</button></div><div id="modalBody"></div></div></div><div class="fixed"><button id="hereBtn" class="primary">⭐ ESTIVE AQUI • CAPTURAR MEMÓRIA</button></div><canvas id="canvas" hidden></canvas>
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script>
'use strict';
const $=id=>document.getElementById(id), clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v)), uid=()=>crypto.randomUUID?crypto.randomUUID():Date.now()+Math.random().toString(16).slice(2); const log=m=>{$('log').textContent='['+new Date().toLocaleTimeString('pt-BR')+'] '+m+'\n'+$('log').textContent.split('\n').slice(0,8).join('\n')}; const toast=m=>{let t=document.createElement('div');t.textContent=m;t.style='position:fixed;left:50%;bottom:70px;transform:translateX(-50%);z-index:200;background:#061018f5;border:1px solid #50d7e8aa;padding:10px 14px;border-radius:12px;max-width:90%;text-align:center';document.body.appendChild(t);setTimeout(()=>t.remove(),2600)};
const R={version:'4.0.0',engine:false,camera:null,gps:null,gpsWatch:null,motion:null,heading:null,battery:null,light:null,audio:null,weather:null,lastPhoto:null,route:null,audioNav:false,consent:{adaptive:false,external:false},records:[]};
let db=null,map=null,userMarker=null,pointsLayer=null,routeLayer=null,recognition=null;
function openDB(){return new Promise((res,rej)=>{const q=indexedDB.open('remina-v40',1);q.onupgradeneeded=e=>{const d=e.target.result;if(!d.objectStoreNames.contains('records'))d.createObjectStore('records',{keyPath:'id'});if(!d.objectStoreNames.contains('settings'))d.createObjectStore('settings',{keyPath:'id'});};q.onsuccess=e=>{db=e.target.result;res(db)};q.onerror=()=>rej(q.error)})}
function put(v){return new Promise((res,rej)=>{const q=db.transaction('records','readwrite').objectStore('records').put(v);q.onsuccess=()=>res();q.onerror=()=>rej(q.error)})} function all(){return new Promise((res,rej)=>{const q=db.transaction('records').objectStore('records').getAll();q.onsuccess=()=>res(q.result||[]);q.onerror=()=>rej(q.error)})}
function delAll(){return new Promise((res,rej)=>{const q=db.transaction('records','readwrite').objectStore('records').clear();q.onsuccess=()=>res();q.onerror=()=>rej(q.error)})}
async function init(){if(R.engine)return;R.engine=true;$('engineDot').classList.add('on');$('engineText').textContent='ON';$('initBtn').disabled=true;$('initBtn').textContent='LABORATÓRIO ATIVO';await openDB();R.records=await all();initMap();renderHistory();renderPoints();updateNet();updateBattery();log('Motor V4.0 iniciado.');toast('REMINA V4.0 ativo.');}
function initMap(){if(map)return;map=L.map('map',{zoomControl:true}).setView([-16.375,-39.58],14);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(map);pointsLayer=L.layerGroup().addTo(map);routeLayer=L.layerGroup().addTo(map)}
function updateNet(){ $('netState').textContent=navigator.onLine?'ONLINE':'OFFLINE'} window.addEventListener('online',updateNet);window.addEventListener('offline',updateNet);
async function startCamera(){try{if(R.camera){$('cameraGate').classList.add('hidden');return}R.camera=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment',width:{ideal:1280},height:{ideal:720}},audio:false});$('camera').srcObject=R.camera;$('cameraGate').classList.add('hidden');log('Câmera ativa.')}catch(e){toast('Não foi possível abrir a câmera.');log('Câmera: '+e.message)}}
function stopCamera(){if(R.camera){R.camera.getTracks().forEach(t=>t.stop());R.camera=null}$('camera').srcObject=null;$('cameraGate').classList.remove('hidden')}
async function flip(){if(!R.camera)return;let old=R.camera.getTracks()[0].getSettings().facingMode||'environment';R.camera.getTracks().forEach(t=>t.stop());try{R.camera=await navigator.mediaDevices.getUserMedia({video:{facingMode:old==='environment'?'user':'environment'},audio:false});$('camera').srcObject=R.camera}catch(e){toast('Troca de câmera indisponível.')}}
async function torch(){if(!R.camera)return toast('Abra a câmera primeiro.');let t=R.camera.getVideoTracks()[0],c=t.getCapabilities?.();if(!c?.torch)return toast('Torch não suportado neste aparelho/navegador.');try{let on=!(t.getSettings().torch);await t.applyConstraints({advanced:[{torch:!on}]});toast(on?'Torch ligado.':'Torch desligado.')}catch(e){toast('Não foi possível controlar o torch.')}}
function photo(){if(!$('camera').videoWidth)return null;let c=$('canvas'),v=$('camera');c.width=v.videoWidth;c.height=v.videoHeight;c.getContext('2d').drawImage(v,0,0);return new Promise(res=>c.toBlob(res,'image/jpeg',.82))}
async function capture(){let blob=await photo();if(!blob)return toast('Abra a câmera primeiro.');R.lastPhoto=blob;await saveRecord('Captura fotográfica');toast('Evidência fotográfica registrada.');}
function getPos(){return R.gps?{...R.gps}:null}
function startGPS(){if(!navigator.geolocation)return toast('GPS não disponível no navegador.');if(R.gpsWatch!=null)return;R.gpsWatch=navigator.geolocation.watchPosition(p=>{R.gps={lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy,heading:p.coords.heading,speed:p.coords.speed,ts:Date.now()};$('gpsHud').textContent='📍 GPS '+R.gps.lat.toFixed(5)+', '+R.gps.lng.toFixed(5)+' ±'+Math.round(R.gps.accuracy)+'m';$('gpsState').textContent='ON';if(!userMarker&&map){userMarker=L.circleMarker([R.gps.lat,R.gps.lng],{radius:7,weight:3}).addTo(map)}else userMarker?.setLatLng([R.gps.lat,R.gps.lng]);if(R.route)updateRouteOverlay();},e=>{toast('GPS: '+e.message);$('gpsState').textContent='ERRO'},{enableHighAccuracy:true,maximumAge:3000,timeout:12000});log('GPS ativado.');toast('GPS ativo.')} 
function battery(){if(!navigator.getBattery)return; navigator.getBattery().then(b=>{R.battery=b.level*100;$('batteryHud').textContent='🔋 '+Math.round(R.battery)+'%';b.addEventListener('levelchange',()=>{$('batteryHud').textContent='🔋 '+Math.round(b.level*100)+'%'});})}
function motion(){if(R.motion)return;const f=e=>{const a=e.accelerationIncludingGravity||e.acceleration||{};const x=a.x||0,y=a.y||0,z=a.z||0;const mag=Math.sqrt(x*x+y*y+z*z);R.motion={x,y,z,mag};$('motionState').textContent=mag>13?'ALTO':mag>10?'MÉDIO':'BAIXO'};window.addEventListener('devicemotion',f);R.motionHandler=f;$('motionState').textContent='ON';log('Movimento ativo.');}
async function orientation(){if(typeof DeviceOrientationEvent==='undefined')return toast('Orientação indisponível.');if(typeof DeviceOrientationEvent.requestPermission==='function'){try{if(await DeviceOrientationEvent.requestPermission()!=='granted')return toast('Permissão de orientação negada.')}catch(e){return toast('Permissão de orientação falhou.')}}const f=e=>{let h=e.webkitCompassHeading??(360-(e.alpha||0));R.heading=(h+360)%360;$('orientationState').textContent=Math.round(R.heading)+'°';};window.addEventListener('deviceorientation',f,true);R.orientationHandler=f;log('Orientação ativa.');}
function startAudio(){if(R.audio)return; navigator.mediaDevices?.getUserMedia({audio:true}).then(stream=>{R.audio=stream;$('audioState').textContent='ON';toast('Microfone ativo para contexto ambiental.');}).catch(()=>toast('Microfone não autorizado.'))}
function speak(t){try{speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance(t))}catch(e){}}
function voice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toast('Reconhecimento de voz indisponível.');if(recognition)return;recognition=new SR();recognition.lang='pt-BR';recognition.interimResults=false;recognition.onstart=()=>{$('voiceStatus').textContent='Ouvindo…';vibrate([40,40])};recognition.onresult=e=>{const t=e.results[0][0].transcript.toLowerCase();$('voiceStatus').textContent='Você disse: '+t;handleVoice(t)};recognition.onerror=()=>{$('voiceStatus').textContent='Não foi possível reconhecer a fala.'};recognition.onend=()=>recognition=null;recognition.start()}
function handleVoice(t){if(t.includes('registr')){$('note').value=t;saveRecord('Voz');return}if(t.includes('analis'))return analyze();if(t.includes('rota')||t.includes('ir '))return openGPS();if(t.includes('onde')){let p=R.gps;let s=p?'Você está em '+p.lat.toFixed(5)+', '+p.lng.toFixed(5):'A localização ainda não está disponível.';speak(s);return}if(t.includes('ler rota')||t.includes('repetir'))return repeatNav();if(t.includes('parar'))return stopRoute();if(t.includes('ajuda'))return $('helpBtn').click();speak('Comando não reconhecido. Diga ajuda para ouvir os comandos disponíveis.')}
const catOpts={Mobilidade:['Calçada bloqueada','Ausência de rampa','Buraco/irregularidade','Travessia difícil','Obstáculo na passagem'],Orientação:['Obstáculo visual','Sinalização insuficiente','Entrada difícil de localizar','Mudança de direção'],Aroma:['Odor forte','Odor desagradável','Possível esgoto','Qualidade ambiental'],Saneamento:['Água suspeita','Alagamento','Esgoto','Resíduo/contaminação'],Infraestrutura:['Rua quebrada','Declive acentuado','Escada','Iluminação insuficiente','Passagem estreita'],Som:['Atividade sonora elevada','Ruído de tráfego','Obra','Som inesperado','Necessário desvio']};
function openCategory(cat){$('modalTitle').textContent=cat+' • registrar';$('modalBody').innerHTML='<p class=\"sub\">Escolha uma observação ou conte a experiência com suas próprias palavras. O usuário confirma antes do registro.</p><div class=\"options\">'+(catOpts[cat]||['Observação geral']).map(x=>`<button class=\"option\" onclick=\"chooseOption(${JSON.stringify(cat)},${JSON.stringify(x)})\">${x}<small>Registrar + analisar contexto</small></button>`).join('')+`<button class=\"option\" onclick=\"openExperience(${JSON.stringify(cat)})\"><b>✍️ Minha experiência</b><small>Escrever livremente, do seu jeito</small></button></div>`;showModal()}
function openExperience(cat){const safeCat=JSON.stringify(cat);$('modalTitle').textContent='✍️ Minha experiência';$('modalBody').innerHTML=`<div class=\"experienceBox\"><p class=\"sub\">Descreva o lugar, o que você percebeu, como foi a experiência e qualquer detalhe que considere importante.</p><textarea id=\"experienceText\" placeholder=\"Exemplo: Encantadora propriedade, onde cantam os pássaros ao amanhecer no extenso arvoredo, cortada por cristalinas e refrescantes águas de um ribeiro. A casa, banhada pelo sol nascente, oferece a sombra tranquila das tardes, na varanda.\"></textarea><div class=\"experienceHint\"><b>Você pode escrever livremente.</b> O REMINA preserva seu relato como experiência humana e pode usar o texto como contexto para uma análise não clínica.</div><div class=\"row\"><button class=\"small\" onclick=\"experienceVoice()\">🎙️ DITAR</button><button class=\"primary\" onclick=\"saveExperience(${safeCat})\">SALVAR EXPERIÊNCIA</button></div></div>`;showModal();setTimeout(()=>$('experienceText')?.focus(),80)}
function experienceVoice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toast('Reconhecimento de voz indisponível.');const r=new SR();r.lang='pt-BR';r.interimResults=false;r.onstart=()=>toast('Fale sua experiência…');r.onresult=e=>{const el=$('experienceText');if(el)el.value+=(el.value?' ':'')+e.results[0][0].transcript};r.onerror=()=>toast('Não foi possível reconhecer a fala.');r.start()}
function saveExperience(cat){const text=$('experienceText')?.value.trim();if(!text){toast('Escreva ou dite sua experiência antes de salvar.');return}$('category').value=cat==='Mobilidade'?'Mobilidade':cat==='Orientação'?'Orientação':cat==='Aroma'?'Aroma':cat==='Saneamento'?'Saneamento':cat==='Infraestrutura'?'Infraestrutura':cat==='Som'?'Som':'Outro';$('note').value=text;hideModal();saveRecord('Minha experiência')}
function showModal(){$('modal').classList.add('show')}function hideModal(){$('modal').classList.remove('show')}
function currentContext(){let text=$('note').value.trim();return{text,gps:getPos(),motion:R.motion,heading:R.heading,battery:R.battery,light:R.light,weather:R.weather,photo:!!R.lastPhoto}}
function analyze(){const c=currentContext(),t=c.text.toLowerCase();let situation='contexto cotidiano',conf=.35,actions=[],impact={mobility:50,orientation:50,environment:50};const rules=[[/buraco|quebrad|calçada|rampa|obstáculo|declive|escada/,'possível barreira de mobilidade',.88,'Verificar rota alternativa', 'mobility'],[/ruído|ruido|barulho|obra|som/,'atividade sonora elevada',.82,'Considerar trecho alternativo ou redução de estímulos','environment'],[/odor|cheiro|esgoto|mau cheiro/,'possível alteração ambiental por odor',.84,'Considerar rota alternativa e registrar evidência','environment'],[/alagamento|chuva|água|agua/,'possível condição ambiental relacionada à água',.72,'Verificar trecho e condições atuais','mobility'],[/perturbador|sobrecarga|incômodo|incomodo|desconforto/,'possível sobrecarga contextual',.62,'Oferecer interface/áudio/vibração conforme preferência','environment']];for(const [re,s,cf,a,d] of rules)if(re.test(t)){situation=s;conf=cf;actions.push(a);impact[d]=Math.round(100-cf*50)}if(c.photo)conf=Math.min(0.96,conf+.08);if(c.gps)conf=Math.min(.98,conf+.04);$('aiSituation').textContent=situation.replace('possível ','');$('aiConfidence').textContent=Math.round(conf*100)+'%';$('aiEvidence').textContent=(c.photo?1:0)+(c.gps?1:0)+(t?1:0)+(c.motion?1:0);$('aiText').textContent='Classificação contextual, não clínica. O sistema separa evidência disponível de inferência.';$('aiActions').innerHTML=actions.length?'Sugestões: '+actions.map(a=>'• '+a).join('<br>'):'Nenhuma ação específica inferida. O usuário decide.';audit(impact,conf);R.analysis={situation,confidence:conf,actions,impact};if((/buraco|quebrad|calçada|rampa|obstáculo|declive/).test(t))suggestRoute();if((/perturbador|sobrecarga|incômodo|incomodo|desconforto/).test(t))adaptivePrompt();toast('Contexto analisado.');log('Análise: '+situation);return R.analysis}
function adaptivePrompt(){if(R.consent.adaptive)return adaptive();$('aiActions').innerHTML+=' <button class="small" onclick="adaptiveConsent()">PERMITIR RESPOSTA ADAPTATIVA</button>'}function adaptiveConsent(){R.consent.adaptive=true;adaptive()}function adaptive(){vibrate([55,70,55]);const msg='Foi percebida uma possível mudança contextual. Você prefere interface simplificada, áudio, vibração ou continuar normalmente?';speak(msg);$('aiActions').innerHTML='<b>Resposta adaptativa disponível.</b><br>Você decide: <button class="small" onclick="setAdaptive(\'simplified\')">SIMPLIFICAR</button> <button class="small" onclick="setAdaptive(\'audio\')">ÁUDIO</button> <button class="small" onclick="setAdaptive(\'normal\')">CONTINUAR</button>'}function setAdaptive(m){if(m==='audio')$('audioNavBtn').click();if(m==='simplified')document.body.style.fontSize='18px';if(m==='normal')document.body.style.fontSize='';toast('Preferência aplicada: '+m)}
function suggestRoute(){if(!R.gps){$('aiActions').innerHTML+=' <button class="small" onclick="startGPS()">ATIVAR GPS PARA ALTERNATIVA</button>';return}const pts=R.records.filter(r=>r.location).map(r=>({r,lat:r.location.lat,lng:r.location.lng}));if(!pts.length)return;let p=pts.sort((a,b)=>distance(R.gps,a)-distance(R.gps,b))[0];setDestination(p.lat,p.lng,p.r.description||'Ponto REMINA')}
function distance(a,b){const Rr=6371000,rad=Math.PI/180,dLat=(b.lat-a.lat)*rad,dLon=(b.lng-a.lng)*rad,la1=a.lat*rad,la2=b.lat*rad;const q=Math.sin(dLat/2)**2+Math.cos(la1)*Math.cos(la2)*Math.sin(dLon/2)**2;return 2*Rr*Math.asin(Math.sqrt(q))}
function setDestination(lat,lng,label){if(!R.gps)return;R.route={origin:{...R.gps},destination:{lat,lng,label},startedAt:Date.now(),mode:'remina'};routeLayer.clearLayers();L.polyline([[R.gps.lat,R.gps.lng],[lat,lng]],{weight:5,dashArray:'10 10'}).addTo(routeLayer);L.circleMarker([lat,lng],{radius:9,weight:3}).addTo(routeLayer);map.fitBounds([[R.gps.lat,R.gps.lng],[lat,lng]],{padding:[20,20]});updateRouteOverlay();updateRouteInfo();speak('Rota REMINA iniciada. '+label+'.');}
function updateRouteInfo(){if(!R.route)return;$('routeInfo').innerHTML='<b>ROTA REMINA ATIVA</b><br>Destino: '+R.route.destination.label+'<br>Distância aproximada: '+Math.round(distance(R.gps,R.route.destination))+' m<br><span class="sub">Rota visual experimental; não é AR calibrado.</span>'}
function updateRouteOverlay(){if(!R.route||!R.gps)return;const d=distance(R.route.origin,R.route.destination);const f=clamp(1-distance(R.gps,R.route.destination)/Math.max(d,1),0,1);const ux=500,uy=850,tx=500,ty=180;const x=ux+(tx-ux)*f,y=uy+(ty-uy)*f;$('routeLine').setAttribute('d',`M ${ux} ${uy} Q 560 500 ${tx} ${ty}`);$('routeUser').setAttribute('cx',ux);$('routeUser').setAttribute('cy',uy);$('routeTarget').setAttribute('cx',tx);$('routeTarget').setAttribute('cy',ty);if(distance(R.gps,R.route.destination)<15){speak('Você chegou ao destino.');toast('Destino alcançado.');}}
function repeatNav(){if(!R.route)return speak('Nenhuma rota ativa.');let d=Math.round(distance(R.gps,R.route.destination));speak('Destino '+R.route.destination.label+'. Aproximadamente '+d+' metros.');}
function stopRoute(){R.route=null;$('routeLine').setAttribute('d','');$('routeUser').setAttribute('cx',-50);$('routeTarget').setAttribute('cx',-50);routeLayer?.clearLayers();$('routeInfo').textContent='Nenhuma rota ativa.';toast('Rota parada.')}
function openGPS(){if(!R.gps)startGPS();showPoints();if(R.gps&&map){map.setView([R.gps.lat,R.gps.lng],16);}}function showPoints(){renderPoints();document.querySelector('#pointList').scrollIntoView({behavior:'smooth'})}
function renderPoints(){if(!pointsLayer||!map)return;pointsLayer.clearLayers();const withLoc=R.records.filter(r=>r.location);$('pointList').innerHTML=withLoc.length?withLoc.slice(-30).reverse().map(r=>`<div class="point"><b>${r.category||'Outro'} — ${r.description||'Sem descrição'}</b><small>${new Date(r.createdAt).toLocaleString('pt-BR')} • ${r.location.lat.toFixed(5)}, ${r.location.lng.toFixed(5)}</small><button class="small" onclick="setDestination(${r.location.lat},${r.location.lng},${JSON.stringify(r.description||'Ponto REMINA')})">IR PARA ESTE PONTO</button></div>`).join(''):'<div class="sub">Nenhum ponto geolocalizado registrado ainda.</div>';withLoc.forEach(r=>L.circleMarker([r.location.lat,r.location.lng],{radius:6,weight:2}).bindTooltip((r.description||r.category||'REMINA')).addTo(pointsLayer));}
async function saveRecord(reason){let desc=$('note').value.trim()||reason||'Observação REMINA';const rec={id:uid(),createdAt:Date.now(),category:$('category').value,description:desc,location:getPos(),sensors:{motion:R.motion,heading:R.heading,battery:R.battery,light:R.light},context:{weather:R.weather},media:{photo:R.lastPhoto?true:false},analysis:null,navigation:R.route?{destination:R.route.destination}:null,privacy:{visibility:'local',consentToShare:false}};if(!rec.location)toast('Registro salvo sem GPS: localização indisponível.');await put(rec);R.records.unshift(rec);R.lastPhoto=null;renderHistory();renderPoints();analyze();$('recordStatus').textContent='Registro local salvo: '+new Date(rec.createdAt).toLocaleTimeString('pt-BR');log('Registro salvo: '+desc)}
function renderHistory(){const h=$('history');h.innerHTML=R.records.slice(0,20).map(r=>`<div class="point"><b>${r.category} — ${r.description}</b><small>${new Date(r.createdAt).toLocaleString('pt-BR')} • ${r.location?'GPS disponível':'sem GPS'} • LOCAL</small></div>`).join('')||'<div class="sub">Nenhum registro ainda.</div>'}
function audit(impact,conf){let mob=impact.mobility||50,ori=impact.orientation||50,env=impact.environment||50;let base=Math.round((mob+ori+env+conf*100)/4);$('scoreMob').textContent=mob;$('scoreOri').textContent=ori;$('scoreEnv').textContent=env;$('auditBar').style.width=base+'%';$('auditText').textContent='Índice experimental de qualidade contextual: '+base+'%. Ausência de registro não significa ausência de barreira.'}
async function weather(){if(!R.gps)return toast('Ative o GPS para clima local.');try{const u=`https://api.open-meteo.com/v1/forecast?latitude=${R.gps.lat}&longitude=${R.gps.lng}&current=temperature_2m,precipitation,wind_speed_10m&timezone=auto`;const j=await fetch(u).then(r=>r.json());R.weather=j.current;$('temp').textContent=j.current.temperature_2m+'°C';$('rain').textContent=j.current.precipitation+' mm';$('wind').textContent=j.current.wind_speed_10m+' km/h';$('weatherHud').textContent='🌦️ '+j.current.temperature_2m+'°C';toast('Clima atualizado.')}catch(e){toast('Clima indisponível sem internet.')}}
function googleMaps(){if(!R.route)return toast('Escolha um destino primeiro.');if(!R.consent.external){R.consent.external=confirm('Abrir navegação externa no Google Maps?');if(!R.consent.external)return}const d=R.route.destination;window.open(`https://www.google.com/maps/dir/?api=1&destination=${d.lat},${d.lng}`,'_blank');}
function audioNav(){R.audioNav=!R.audioNav;$('audioNavBtn').textContent='🔊 ÁUDIO: '+(R.audioNav?'ON':'OFF');if(R.audioNav)repeatNav()}
function vibrate(p=[45]){try{navigator.vibrate?.(p)}catch(e){}}
function updateBattery(){if(navigator.getBattery)navigator.getBattery().then(b=>{$('batteryHud').textContent='🔋 '+Math.round(b.level*100)+'%'})}
function exportJSON(){const blob=new Blob([JSON.stringify({app:'REMINA Mobile Sensor Lab',version:R.version,exportedAt:new Date().toISOString(),records:R.records},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='remina-mobile-sensor-lab-v4-records.json';a.click();URL.revokeObjectURL(a.href)}
function noteVoice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toast('Voz indisponível.');const x=new SR();x.lang='pt-BR';x.onresult=e=>{$('note').value=e.results[0][0].transcript};x.start()}
$('initBtn').onclick=init;$('openCamera').onclick=startCamera;$('stopCamBtn').onclick=stopCamera;$('flipBtn').onclick=flip;$('torchBtn').onclick=torch;$('captureBtn').onclick=capture;$('analyzeBtn').onclick=analyze;$('contextBtn').onclick=analyze;$('adaptiveBtn').onclick=adaptivePrompt;$('gpsBtn').onclick=startGPS;$('pointsBtn').onclick=showPoints;$('googleBtn').onclick=googleMaps;$('audioNavBtn').onclick=audioNav;$('repeatNavBtn').onclick=repeatNav;$('stopRouteBtn').onclick=stopRoute;$('voiceBtn').onclick=voice;$('speakBtn').onclick=()=>speak(R.route?'Rota ativa para '+R.route.destination.label:'Nenhuma rota ativa.');$('saveBtn').onclick=()=>saveRecord('Nota manual');$('voiceNoteBtn').onclick=noteVoice;$('hereBtn').onclick=async()=>{await capture().catch(()=>{});if(!R.lastPhoto)await saveRecord('Estive aqui');toast('Memória contextual capturada.');};$('weatherBtn').onclick=weather;$('exportBtn').onclick=exportJSON;$('clearBtn').onclick=async()=>{if(confirm('Apagar a memória local do REMINA?')){await delAll();R.records=[];renderHistory();renderPoints();toast('Memória local limpa.')}};$('modalClose').onclick=hideModal;$('modal').onclick=e=>{if(e.target.id==='modal')hideModal()};$('helpBtn').onclick=()=>{showModal();$('modalTitle').textContent='Como funciona';$('modalBody').innerHTML='<p><b>1.</b> Inicie o laboratório.<br><b>2.</b> Abra câmera/GPS conforme necessário.<br><b>3.</b> Registre ocorrências com foto, texto e localização.<br><b>4.</b> Analise o contexto.<br><b>5.</b> Escolha uma alternativa.<br><b>6.</b> Audite o resultado.</p><p class="sub">A análise é experimental e não clínica. O REMINA auxilia; o usuário decide. Recursos externos dependem de internet e permissões.</p>'};
window.openCategory=openCategory;window.chooseOption=chooseOption;window.setDestination=setDestination;window.startGPS=startGPS;window.openGPS=openGPS;window.repeatNav=repeatNav;window.stopRoute=stopRoute;window.adaptiveConsent=adaptiveConsent;window.setAdaptive=setAdaptive;
(async()=>{try{await openDB();R.records=await all();renderHistory()}catch(e){$('storageState').textContent='FALLBACK';}updateNet();updateBattery();})();
</script>
<script>
/* REMINA Mobile Sensor Lab V4.1 — integrated foundation/media/experience/gps/sensors/analysis/response/memory/audit */
(()=>{
'use strict';
const V=window.REMINA_V41={version:'4.1',dbName:'remina-v41',dbVersion:1,db:null,mediaStream:null,videoRecorder:null,videoChunks:[],audioStream:null,audioRecorder:null,audioChunks:[],gpsWatch:null,gps:null,route:null,recordId:null,mediaIds:[],state:'READY',lastAnalysis:null,cooldowns:{}};
const $=id=>document.getElementById(id); const now=()=>Date.now(); const uid=p=>(p||'ID')+'-'+now().toString(36)+'-'+Math.random().toString(36).slice(2,7);
function logV(m){try{if(window.log)window.log('[V4.1] '+m); else console.log('[V4.1]',m)}catch(e){console.log(m)}}
function toastV(m){try{if(window.toast)window.toast(m);else alert(m)}catch(e){alert(m)}}
function openDB(){return new Promise((res,rej)=>{const q=indexedDB.open(V.dbName,V.dbVersion);q.onupgradeneeded=e=>{const d=e.target.result; for(const n of ['records','media','routes','analyses','audits','settings','syncQueue']) if(!d.objectStoreNames.contains(n)) d.createObjectStore(n,{keyPath:'id'});};q.onsuccess=()=>{V.db=q.result;res(q.result)};q.onerror=()=>rej(q.error)})}
function put(store,obj){return new Promise((res,rej)=>{const t=V.db.transaction(store,'readwrite');t.objectStore(store).put(obj);t.oncomplete=()=>res(obj);t.onerror=()=>rej(t.error)})}
function getAll(store){return new Promise((res,rej)=>{const t=V.db.transaction(store,'readonly'),r=t.objectStore(store).getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}
function getOne(store,id){return new Promise((res,rej)=>{const r=V.db.transaction(store,'readonly').objectStore(store).get(id);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}
function delOne(store,id){return new Promise((res,rej)=>{const t=V.db.transaction(store,'readwrite');t.objectStore(store).delete(id);t.oncomplete=()=>res();t.onerror=()=>rej(t.error)})}
async function init(){await openDB(); if($('engineText'))$('engineText').textContent='V4.1 • LOCAL / OFFLINE-FIRST'; if($('engineDot'))$('engineDot').classList.add('ok'); await restore(); bind(); logV('V4.1 pronta');}
async function restore(){const rs=await getAll('records'); if(window.R){R.records=rs.sort((a,b)=>b.createdAt-a.createdAt);if(window.renderHistory)renderHistory();if(window.renderPoints)renderPoints()} const routes=await getAll('routes');V.route=routes.find(x=>x.status==='ACTIVE')||null; updateStates();}
function bind(){
 const b=(id,fn)=>$(id)?.addEventListener('click',fn);
 b('openCamera',startCamera); b('stopCamBtn',stopCamera); b('captureBtn',capturePhoto); b('flipBtn',flipCamera); b('torchBtn',toggleTorch); b('videoBtn',toggleVideo); b('voiceNoteBtn',toggleAudioEvidence); b('saveBtn',saveCurrentExperience); b('contextBtn',captureContext); b('adaptiveBtn',adaptiveResponse); b('gpsBtn',startGPS); b('quickGPSBtn',startGPS); b('googleBtn',openGoogleMaps); b('audioNavBtn',startVoiceNav); b('repeatNavBtn',repeatVoiceNav); b('stopRouteBtn',stopRoute); b('exportBtn',exportAll); b('clearBtn',clearAll); b('weatherBtn',weatherContext); b('hereBtn',saveHere); b('experienceBtn',()=>experienceSheet(''));
 window.addEventListener('beforeunload',()=>{try{stopCamera();stopGPS()}catch(e){}});
}
async function startCamera(){try{if(V.mediaStream)stopCamera(); const facing=(V.facing||'environment'); V.mediaStream=await navigator.mediaDevices.getUserMedia({video:{facingMode:facing,width:{ideal:1280},height:{ideal:720}},audio:false}); $('camera').srcObject=V.mediaStream; $('camera').style.display='block'; $('cameraGate')?.classList.add('hide'); V.state='CAMERA_READY'; logV('camera ready')}catch(e){toastV('Câmera indisponível ou permissão negada.');audit('CAMERA_ERROR',e.message)}}
function stopCamera(){if(V.mediaStream){V.mediaStream.getTracks().forEach(t=>t.stop());V.mediaStream=null}if($('camera'))$('camera').srcObject=null; if($('cameraGate'))$('cameraGate').classList.remove('hide');}
async function flipCamera(){V.facing=V.facing==='user'?'environment':'user';await startCamera()}
async function toggleTorch(){const t=V.mediaStream?.getVideoTracks?.()[0];if(!t)return toastV('Abra a câmera primeiro.');try{const c=t.getCapabilities?.();if(!c?.torch)return toastV('Lanterna não disponível neste aparelho/navegador.');V.torch=!V.torch;await t.applyConstraints({advanced:[{torch:V.torch}]})}catch(e){toastV('Não foi possível controlar a lanterna.')}}
function canvasBlob(){return new Promise(resolve=>{const v=$('camera'),c=$('canvas');if(!v||!V.mediaStream)return resolve(null);c.width=v.videoWidth||1280;c.height=v.videoHeight||720;c.getContext('2d').drawImage(v,0,0,c.width,c.height);c.toBlob(resolve,'image/jpeg',.88)})}
async function saveMedia(blob,type,extra={}){if(!blob)return null;const id=uid('MED');await put('media',{id,type,mimeType:blob.type||'',size:blob.size||0,blob,createdAt:now(),source:extra.source||'CAMERA',recordId:extra.recordId||null,location:V.gps||null,duration:extra.duration||0,width:extra.width||null,height:extra.height||null});return id}
async function capturePhoto(){const blob=await canvasBlob();if(!blob)return toastV('Abra a câmera primeiro.');const id=await saveMedia(blob,'PHOTO');V.mediaIds.push(id);V.lastPhotoId=id; $('aiEvidence')&&($('aiEvidence').textContent='Foto salva localmente');audit('PHOTO_CAPTURED',id);toastV('Foto salva no armazenamento local.');}
async function toggleVideo(){if(V.videoRecorder){V.videoRecorder.stop();return} if(!V.mediaStream)return toastV('Abra a câmera primeiro.');if(!window.MediaRecorder)return toastV('Vídeo não é suportado neste navegador.');try{V.videoChunks=[];const mime=MediaRecorder.isTypeSupported('video/webm;codecs=vp9')?'video/webm;codecs=vp9':'video/webm';V.videoRecorder=new MediaRecorder(V.mediaStream,{mimeType:mime});const started=now();V.videoRecorder.ondataavailable=e=>e.data.size&&V.videoChunks.push(e.data);V.videoRecorder.onstop=async()=>{const blob=new Blob(V.videoChunks,{type:mime});const id=await saveMedia(blob,'VIDEO',{duration:now()-started});V.mediaIds.push(id);V.videoRecorder=null;V.videoChunks=[];audit('VIDEO_SAVED',id);toastV('Vídeo salvo localmente.');updateStates()};V.videoRecorder.start(500);toastV('Gravação de vídeo iniciada. Toque novamente para parar.');audit('VIDEO_STARTED')}catch(e){V.videoRecorder=null;toastV('Não foi possível iniciar o vídeo.')}}
async function toggleAudioEvidence(){if(V.audioRecorder){V.audioRecorder.stop();return}try{if(!V.audioStream)V.audioStream=await navigator.mediaDevices.getUserMedia({audio:true});if(!window.MediaRecorder)return toastV('Gravação de áudio não suportada.');V.audioChunks=[];const mime=MediaRecorder.isTypeSupported('audio/webm')?'audio/webm':'';V.audioRecorder=new MediaRecorder(V.audioStream,mime?{mimeType:mime}:undefined);const started=now();V.audioRecorder.ondataavailable=e=>e.data.size&&V.audioChunks.push(e.data);V.audioRecorder.onstop=async()=>{const blob=new Blob(V.audioChunks,{type:mime||'audio/webm'});const id=await saveMedia(blob,'AUDIO',{duration:now()-started,source:'HUMAN_EVIDENCE'});V.mediaIds.push(id);V.audioRecorder=null;toastV('Áudio de evidência salvo.');audit('AUDIO_SAVED',id);};V.audioRecorder.start(400);toastV('Áudio de evidência iniciado. Toque novamente para parar.')}catch(e){toastV('Microfone não autorizado.')}}
function currentPos(){return V.gps?{...V.gps}:((window.R&&R.lastGPS)?{...R.lastGPS}:null)}
function startGPS(){if(!navigator.geolocation)return toastV('GPS não disponível.');if(V.gpsWatch!==null)return toastV('GPS já está ativo.');navigator.geolocation.getCurrentPosition(onGPS,e=>{audit('GPS_ERROR',e.message);toastV('Não foi possível obter GPS agora.')},{enableHighAccuracy:true,timeout:12000,maximumAge:3000});V.gpsWatch=navigator.geolocation.watchPosition(onGPS,e=>{if($('gpsState'))$('gpsState').textContent='LOST';audit('GPS_LOST',e.message)},{enableHighAccuracy:true,timeout:15000,maximumAge:3000});toastV('GPS contínuo ativado.')}
function onGPS(p){const c=p.coords;V.gps={lat:c.latitude,lon:c.longitude,accuracy:c.accuracy,altitude:c.altitude,speed:c.speed,heading:c.heading,timestamp:p.timestamp,quality:c.accuracy<=10?'HIGH':c.accuracy<=35?'MEDIUM':'LOW'};if(window.R){R.lastGPS=V.gps;R.lat=V.gps.lat;R.lng=V.gps.lon} if($('gpsState'))$('gpsState').textContent='ACTIVE • ±'+Math.round(c.accuracy)+'m';if($('gpsHud'))$('gpsHud').textContent=V.gps.lat.toFixed(5)+', '+V.gps.lon.toFixed(5);updateRoute();}
function stopGPS(){if(V.gpsWatch!==null)navigator.geolocation.clearWatch(V.gpsWatch);V.gpsWatch=null;if($('gpsState'))$('gpsState').textContent='OFF'}
function sensorSnapshot(){return {gps:V.gps||null,motion:window.R?.motion||null,heading:window.R?.heading||null,battery:window.R?.battery||null,light:window.R?.light||null,timestamp:now()}}
function contextSnapshot(){return {location:V.gps||null,movement:window.R?.motion||null,orientation:window.R?.heading||null,camera:{active:!!V.mediaStream},audio:{active:!!V.audioStream},battery:window.R?.battery||null,weather:window.R?.weather||null,connectivity:{online:navigator.onLine},timestamp:now()}}
async function captureContext(){const ctx=contextSnapshot();await put('settings',{id:'lastContext',value:ctx});if($('aiText'))$('aiText').textContent='Contexto capturado: '+new Date().toLocaleTimeString('pt-BR');toastV('Contexto capturado.');audit('CONTEXT_SNAPSHOT')}
function categoryValue(){return $('category')?.value||''}
function textValue(){return $('note')?.value?.trim()||''}
function showExperience(cat=''){const title='✍️ Minha experiência';const body=`<div class="v41-sheet"><div class="v41-card"><b>Relato humano — preservado como escrito</b><p class="v41-muted">Registrar primeiro, analisar depois. O texto abaixo não é reescrito pela IA.</p><textarea id="v41ExperienceText" aria-label="Minha experiência" placeholder="Conte o que você percebeu, o que aconteceu e qualquer detalhe importante."></textarea><div class="v41-grid"><button class="small" id="v41Dictate">🎙️ DITAR</button><button class="primary" id="v41SaveExp">SALVAR</button></div><div class="v41-muted" id="v41DictStatus"></div></div></div>`;if(window.showModal){$('modalTitle').textContent=title;$('modalBody').innerHTML=body;showModal()}else{const t=prompt('Minha experiência:','');if(t)saveExperienceV41(cat,t);return} $('v41Dictate').onclick=()=>dictate('v41ExperienceText','v41DictStatus');$('v41SaveExp').onclick=()=>saveExperienceV41(cat,$('v41ExperienceText').value);}
function experienceSheet(cat){showExperience(cat)}
async function saveExperienceV41(cat,text){if(!text.trim())return toastV('Escreva ou dite sua experiência antes de salvar.');await saveExperienceCore({category:cat,description:text.trim(),inputMethod:'TEXT',source:'HUMAN'});if(window.closeModal)closeModal();}
async function saveCurrentExperience(){const txt=textValue();const cat=categoryValue();if(!txt)return experienceSheet(cat);await saveExperienceCore({category:cat,description:txt,inputMethod:'TEXT',source:'HUMAN'});}
async function saveExperienceCore(x){const rec={id:uid('EXP'),createdAt:now(),status:'SAVED',category:x.category||null,description:x.description,source:'HUMAN',inputMethod:x.inputMethod||'TEXT',mediaIds:[...V.mediaIds],location:currentPos(),sensors:sensorSnapshot(),context:contextSnapshot(),analysisId:null,privacy:{visibility:'local',consentToShare:false}};await put('records',rec);for(const mid of rec.mediaIds){const m=await getOne('media',mid);if(m){m.recordId=rec.id;await put('media',m)}};V.mediaIds=[];V.recordId=rec.id;const analysis=analyzeRecord(rec);analysis.recordId=rec.id;await put('analyses',analysis);rec.analysisId=analysis.id;rec.status='ANALYZED';await put('records',rec);await audit('RECORD_SAVED',rec.id);if(window.R){R.records=R.records||[];R.records.unshift(rec);if(window.renderHistory)renderHistory();if(window.renderPoints)renderPoints()} renderAnalysis(analysis);toastV('Experiência salva localmente.');return rec}
function analyzeRecord(rec){const obs=[],inf=[],unc=[],conf=[];if(rec.description)obs.push({statement:'Relato humano registrado sem alteração.',source:'HUMAN',confidence:'HIGH'});if(rec.location)obs.push({statement:'Localização disponível no momento do registro.',source:'GPS',confidence:rec.location.quality||'UNKNOWN'});else unc.push({statement:'Localização indisponível.',confidence:'HIGH'});if(rec.mediaIds?.length)obs.push({statement:rec.mediaIds.length+' evidência(s) de mídia associada(s).',source:'MEDIA',confidence:'HIGH'});const score=accessScore(rec);if(score.confidence!=='UNKNOWN')inf.push({statement:'Estimativa contextual de acessibilidade disponível; não é certificação.',basedOn:['HUMAN','GPS','SENSOR'],confidence:score.confidence});return {id:uid('AN'),recordId:rec.id,observed:obs,inferred:inf,uncertain:unc,conflicts:conf,recommendations:['Validar qualquer conclusão no local antes de tomar decisão.'],confidence:obs.length?'MEDIUM':'LOW',accessibility:score,createdAt:now(),source:'SYSTEM'} }
function accessScore(rec){const base=50;let s=base;if(rec.location)s+=10;if(rec.mediaIds?.length)s+=10;if(rec.description?.length>40)s+=10;return {score:Math.max(0,Math.min(100,s)),dimensions:{mobility:s,orientation:s,sound:s,environment:s},confidence:(rec.location?'MEDIUM':'LOW'),evidenceCount:(rec.mediaIds?.length||0)+1,timestamp:now(),note:'Estimativa REMINA; não é certificação oficial.'}}
function renderAnalysis(a){if(!$('aiSituation'))return;$('aiSituation').textContent='ANÁLISE V4.1';$('aiConfidence').textContent='Confiança: '+a.confidence;$('aiEvidence').textContent='Evidências: '+(a.observed.length);$('aiText').textContent=a.inferred.map(x=>x.statement).join(' ')||'Nenhuma inferência adicional.';if($('scoreMob'))$('scoreMob').textContent=a.accessibility.score;$('scoreOri')&&($('scoreOri').textContent=a.accessibility.score);$('scoreEnv')&&($('scoreEnv').textContent=a.accessibility.score)}
function adaptiveResponse(){const text='Resposta adaptativa: contexto considerado sem interromper desnecessariamente. Ação visual disponível; voz/háptica somente com consentimento.';if($('aiText'))$('aiText').textContent=text; if(navigator.vibrate)navigator.vibrate([30]); if(window.speechSynthesis&&localStorage.getItem('remina_voice_consent')==='1')speechSynthesis.speak(new SpeechSynthesisUtterance(text));audit('ADAPTIVE_RESPONSE')}
function dictate(inputId,statusId){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toastV('Ditado não disponível neste navegador.');const r=new SR();r.lang='pt-BR';r.interimResults=false;r.maxAlternatives=1;r.onstart=()=>$(statusId)&&($(statusId).textContent='Ouvindo…');r.onresult=e=>{$(inputId).value+=(($(inputId).value?' ':'')+e.results[0][0].transcript);};r.onerror=()=>$(statusId)&&($(statusId).textContent='Não foi possível concluir o ditado.');r.onend=()=>$(statusId)&&($(statusId).textContent='');r.start()}
function startVoiceNav(){if(!V.route)return toastV('Inicie uma rota primeiro.');localStorage.setItem('remina_voice_consent','1');speakNav();}
function repeatVoiceNav(){speakNav()}
function speakNav(){if(!V.route)return;const p=V.gps;const d=distance(p?.lat,p?.lon,V.route.destination?.lat,V.route.destination?.lon);const msg=p&&d!=null?`Destino a aproximadamente ${Math.round(d)} metros. Precisão do GPS ${Math.round(p.accuracy)} metros.`:'GPS indisponível no momento.';if(window.speechSynthesis){speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance(msg))}toastV(msg)}
function startRoute(dest){if(!dest&&V.gps)dest={lat:V.gps.lat,lon:V.gps.lon};if(!dest)return toastV('Destino sem coordenadas.');V.route={id:uid('ROUTE'),origin:V.gps?{...V.gps}:null,destination:dest,waypoints:[],status:'ACTIVE',startedAt:now(),accessibilityEstimate:null};put('routes',V.route);audit('ROUTE_STARTED',V.route.id);toastV('Rota REMINA iniciada.')}
function updateRoute(){if(!V.route||V.route.status!=='ACTIVE'||!V.gps)return;const d=distance(V.gps.lat,V.gps.lon,V.route.destination.lat,V.route.destination.lon);if($('routeInfo'))$('routeInfo').textContent='Destino: '+Math.round(d)+' m • GPS ±'+Math.round(V.gps.accuracy)+' m';if(d<=Math.max(12,V.gps.accuracy*1.25)){V.route.status='ARRIVED';V.route.arrivedAt=now();put('routes',V.route);audit('ROUTE_ARRIVED',V.route.id);toastV('Destino alcançado dentro da tolerância do GPS.')}}
function stopRoute(){if(V.route){V.route.status='STOPPED';put('routes',V.route);audit('ROUTE_STOPPED',V.route.id);V.route=null;toastV('Rota encerrada.')}}
function openGoogleMaps(){if(!V.gps)return toastV('Ative o GPS antes.');const u='https://www.google.com/maps/dir/?api=1&destination='+encodeURIComponent(V.gps.lat+','+V.gps.lon);window.open(u,'_blank','noopener');audit('GOOGLE_MAPS_OPENED')}
function distance(a,b,c,d){if([a,b,c,d].some(x=>typeof x!=='number'))return null;const Rr=6371000,rad=Math.PI/180;const p1=a*rad,p2=c*rad,dp=(c-a)*rad,dq=(d-b)*rad;const x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dq/2)**2;return 2*Rr*Math.asin(Math.sqrt(x))}
async function saveHere(){const p=currentPos();if(!p)return toastV('GPS indisponível.');await saveExperienceCore({category:'LOCALIZAÇÃO',description:'Estive aqui — ponto contextual REMINA.',inputMethod:'GPS',source:'HUMAN'});}
async function weatherContext(){try{if(!V.gps)return toastV('Ative o GPS para consultar clima contextual.');const u=`https://api.open-meteo.com/v1/forecast?latitude=${V.gps.lat}&longitude=${V.gps.lon}&current=temperature_2m,wind_speed_10m,precipitation`;const r=await fetch(u);const j=await r.json();V.weather=j.current; if($('temp'))$('temp').textContent=j.current?.temperature_2m??'—';if($('wind'))$('wind').textContent=j.current?.wind_speed_10m??'—';if($('rain'))$('rain').textContent=j.current?.precipitation??'—';toastV('Clima contextual atualizado.');audit('WEATHER_UPDATED')}catch(e){toastV('Clima indisponível; app continua offline.')}}
async function audit(type,data){try{await put('audits',{id:uid('AUD'),type,data:data||null,createdAt:now(),source:'SYSTEM'})}catch(e){}}
async function exportAll(){const out={version:'4.1',exportedAt:now(),records:await getAll('records'),media:(await getAll('media')).map(m=>({...m,blob:undefined})),routes:await getAll('routes'),analyses:await getAll('analyses'),audits:await getAll('audits')};const blob=new Blob([JSON.stringify(out,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='remina-v41-backup-'+new Date().toISOString().slice(0,10)+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);audit('EXPORT_JSON')}
async function clearAll(){if(!confirm('Apagar a memória local do REMINA V4.1? Esta ação não pode ser desfeita.'))return;for(const s of ['records','media','routes','analyses','audits','syncQueue']){for(const x of await getAll(s))await delOne(s,x.id)}V.mediaIds=[];V.route=null;V.recordId=null;if(window.renderHistory)renderHistory();toastV('Memória local apagada.');audit('MEMORY_CLEARED')}
function updateStates(){if($('storageState'))$('storageState').textContent='IndexedDB V4.1';if($('netState'))$('netState').textContent=navigator.onLine?'ONLINE':'OFFLINE';if($('audioState'))$('audioState').textContent=V.audioStream?'ON':'OFF'}
window.openExperience=(cat)=>experienceSheet(cat||'');window.openCategory=(cat)=>{if(window.showModal){$('modalTitle').textContent=cat+' • registrar';$('modalBody').innerHTML='<div class="v41-sheet"><div class="v41-card"><p>Escolha uma opção ou registre sua experiência livremente.</p><button class="option" id="v41Free">✍️ Minha experiência<small>Canal humano livre</small></button></div></div>';showModal();$('v41Free').onclick=()=>experienceSheet(cat)}};
window.saveExperienceV41=saveExperienceV41;window.startV41Route=startRoute;window.captureV41Context=captureContext;
window.addEventListener('online',updateStates);window.addEventListener('offline',updateStates);
if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./service-worker.js').catch(()=>{}))} if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>init().catch(e=>console.error(e)));else init().catch(e=>console.error(e));
})();
</script>
</body></html>